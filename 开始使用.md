# 🚀 快速开始指南

## 第一步：环境准备

### 1. 创建虚拟环境并安装依赖

```bash
# 方式1：使用快速启动脚本（Windows推荐）
quick_start.bat

# 方式2：手动安装
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

### 2. 配置环境变量

```bash
# 复制模板文件
copy .env_template .env

# 编辑 .env 文件，填入以下配置：
# DASHSCOPE_API_KEY=你的通义千问API密钥
# NEO4J_PASSWORD=你的Neo4j数据库密码
```

**获取API密钥：**
- 访问 https://dashscope.aliyun.com/
- 注册并创建API Key

### 3. 启动Neo4j数据库

**方式1：Docker（推荐）**
```bash
docker run -d --name neo4j -p 7474:7474 -p 7687:7687 -e NEO4J_AUTH=neo4j/your_password neo4j:latest
```

**方式2：Neo4j Desktop**
- 下载：https://neo4j.com/download/
- 安装并启动数据库

**验证：**
- 访问 http://localhost:7474
- 使用用户名 `neo4j` 和你设置的密码登录

## 第二步：测试环境

```bash
python test_connection.py
```

如果所有测试通过，说明环境配置成功！✅

## 第三步：运行分析

```bash
python main_pipeline.py
```

**输出内容：**
1. 控制台显示分析进度
2. 生成 `analysis_result_YYYYMMDD_HHMMSS.json` 文件
3. Neo4j中构建知识图谱

## 第四步：查看结果

### 方式1：查看分析报告

```bash
python visualizer.py
```

### 方式2：Neo4j图形界面

1. 访问 http://localhost:7474
2. 执行查询：

```cypher
// 查看整体结构
MATCH (n)-[r]->(m) RETURN n, r, m LIMIT 100

// 查看事件详情
MATCH (e:Event)-[:处于]->(p:OpinionPhase) RETURN e, p
```

## 常用命令

```bash
# 测试各模块
python data_parser.py      # 测试数据解析
python llm_analyzer.py     # 测试LLM分析
python kg_builder.py       # 测试Neo4j连接

# 生成报告
python visualizer.py       # 生成文字报告和数据导出
```

## 遇到问题？

### 问题1：找不到 .env 文件
```bash
copy .env_template .env
notepad .env  # 编辑配置
```

### 问题2：Neo4j连接失败
- 确认Neo4j已启动
- 检查密码是否正确
- 确认端口7687未被占用

### 问题3：API调用失败
- 检查API Key是否正确
- 确认账户余额充足
- 测试网络连接

### 问题4：依赖安装失败
```bash
# 升级pip
python -m pip install --upgrade pip

# 使用清华源
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

## 详细文档

- 📖 **README.md** - 项目总览和快速开始
- 📖 **install_guide.md** - 详细安装指南
- 📖 **usage_examples.md** - 丰富的使用示例
- 📖 **项目说明.md** - 完整的技术文档

## 核心文件说明

| 文件 | 功能 |
|-----|------|
| `config.py` | 配置管理 |
| `data_parser.py` | 数据解析 |
| `llm_analyzer.py` | LLM分析 |
| `kg_builder.py` | 图谱构建 |
| `main_pipeline.py` | 主程序 |
| `visualizer.py` | 可视化 |
| `test_connection.py` | 环境测试 |

## 典型工作流

```
1. 准备数据 → 放入 weibo_comments_full.json
2. 配置环境 → 编辑 .env 文件
3. 测试连接 → python test_connection.py
4. 运行分析 → python main_pipeline.py
5. 查看结果 → python visualizer.py 或 Neo4j Browser
```

## 下一步

✅ 环境配置完成后，可以：
1. 尝试分析示例数据
2. 准备自己的微博数据
3. 根据需求修改分析逻辑
4. 扩展知识图谱结构

祝使用愉快！🎉

